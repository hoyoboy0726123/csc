version: "3.9"

services:
  aicms:
    build: .
    container_name: aicms
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    command: python main.py